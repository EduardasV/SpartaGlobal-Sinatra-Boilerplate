<h1>Welcome to the books page</h1>
<% @books.each do |book| %>
  <div>
    <h3>
      <a href="/books/<%= book[:id] %>"><%= book[:title] %></a>
    </h3>
    <p><%= book[:book_body] %></p>
    <img src="<%= book[:image] %>" height="140" width="85">
  </div>
<% end %>
